node_modules
# Keep environment variables out of version control
.env

/generated/prisma
/docs/worklog
dist
coverage/

# Only the public database CA is allowed in this directory.
certs/*
!certs/supabase-ca.crt
certs/*.pem
certs/*.private-key

# Local deployment rollback snapshots may contain AWS resource identifiers.
/deploy/rollback/

# Local production database backups contain sensitive data.
/backups/

# Local deployment documentation
/docs/deployment/
